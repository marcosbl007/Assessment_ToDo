/**
 * Capa de servicios del frontend.
 * - Centralizar llamadas HTTP hacia backend (auth, tareas, aprobaciones, etc.).
 * - Mantiene separada la lógica de red respecto de componentes UI.
 */

export {};
